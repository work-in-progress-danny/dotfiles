# install nix
# curl -L https://nixos.org/nix/install | sh

echo "installing"

echo "copying sample gitconfig, you'll need to update user information"

cp ./.sample_gitconfig ~/.gitconfig



# nixpkgs.starship Starship is broken at the moment so installing it manually is the only option

# curl -fsSL https://starship.rs/install.sh | sh -c 
